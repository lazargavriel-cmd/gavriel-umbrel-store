version: "3.7"

services:

  app_proxy:
    environment:
      APP_HOST: gavriel-bot-control-center_web_1
      APP_PORT: 80

  web:
    image: nginx:1.25.3
    restart: on-failure
    stop_grace_period: 30s

    volumes:
      - /data/bot-control-center/public:/usr/share/nginx/html:ro
